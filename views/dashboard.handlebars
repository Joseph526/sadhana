<div class="dashboard">
    <div class="container-fluid">
        <div class="row">
            {{> sidebar }}
                <div class="main">
                    {{> sadha }}
                </div>
                <div class="main">
                    {{> today }}
                </div>
            </main>
        </div>
    </div>

</div>

{{> footer}}